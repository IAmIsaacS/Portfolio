<section
  id="contact"
  class="relative bg-black py-16 md:py-20 overflow-hidden border-t border-gray-800/30"
>
  <div class="max-w-4xl mx-auto px-5 text-center">
    <!-- Título con animación -->
    <h2
      class="text-2xl md:text-3xl font-light text-white uppercase tracking-wider mb-4 md:mb-6 opacity-0 contacto-title"
    >
      Conectemos
    </h2>

    <!-- Subtítulo -->
    <p
      class="text-gray-400 text-sm md:text-base max-w-lg mx-auto mb-8 md:mb-10 opacity-0 contacto-subtitle"
    >
      Estoy abierto a oportunidades y colaboraciones. Siéntete libre de
      contactarme a través de mis redes.
    </p>

    <!-- Contenedor de botones - Modificado para móviles -->
    <div
      class="flex flex-col md:flex-row items-center justify-center gap-3 md:gap-6"
    >
      <!-- Botón LinkedIn -->
      <a
        href="https://www.linkedin.com/in/iamisaacs/"
        target="_blank"
        rel="noopener noreferrer"
        class="w-full md:w-auto contacto-item opacity-0 transform translate-y-6"
      >
        <button
          class="w-full md:w-auto flex items-center justify-center gap-2 px-6 py-3 bg-transparent border border-gray-700 rounded-lg text-white hover:bg-[#0A66C2]/10 hover:border-[#0A66C2]/50 transition-all duration-300 group"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            viewBox="0 0 24 24"
            class="w-5 h-5 text-white group-hover:text-[#0A66C2] transition-colors"
          >
            <path
              fill="currentColor"
              d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037c-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85c3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 0 1-2.063-2.065a2.064 2.064 0 1 1 2.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"
            ></path>
          </svg>
          <span
            class="text-sm md:text-base font-light group-hover:text-[#0A66C2] transition-colors"
            >LinkedIn</span
          >
        </button>
      </a>

      <!-- Botón GitHub -->
      <a
        href="https://github.com/IAmIsaacS"
        target="_blank"
        rel="noopener noreferrer"
        class="w-full md:w-auto contacto-item opacity-0 transform translate-y-6"
      >
        <button
          class="w-full md:w-auto flex items-center justify-center gap-2 px-6 py-3 bg-transparent border border-gray-700 rounded-lg text-white hover:bg-gray-700/10 hover:border-gray-500 transition-all duration-300 group"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            viewBox="0 0 24 24"
            class="w-5 h-5 text-white transition-colors"
          >
            <path
              fill="currentColor"
              d="M12 .297c-6.63 0-12 5.373-12 12c0 5.303 3.438 9.8 8.205 11.385c.6.113.82-.258.82-.577c0-.285-.01-1.04-.015-2.04c-3.338.724-4.042-1.61-4.042-1.61C4.422 18.07 3.633 17.7 3.633 17.7c-1.087-.744.084-.729.084-.729c1.205.084 1.838 1.236 1.838 1.236c1.07 1.835 2.809 1.305 3.495.998c.108-.776.417-1.305.76-1.605c-2.665-.3-5.466-1.332-5.466-5.93c0-1.31.465-2.38 1.235-3.22c-.135-.303-.54-1.523.105-3.176c0 0 1.005-.322 3.3 1.23c.96-.267 1.98-.399 3-.405c1.02.006 2.04.138 3 .405c2.28-1.552 3.285-1.23 3.285-1.23c.645 1.653.24 2.873.12 3.176c.765.84 1.23 1.91 1.23 3.22c0 4.61-2.805 5.625-5.475 5.92c.42.36.81 1.096.81 2.22c0 1.606-.015 2.896-.015 3.286c0 .315.21.69.825.57C20.565 22.092 24 17.592 24 12.297c0-6.627-5.373-12-12-12"
            ></path>
          </svg>
          <span class="text-sm md:text-base font-light transition-colors"
            >GitHub</span
          >
        </button>
      </a>

      <!-- Botón Curriculum (rojo) -->
      <a
        href="\CurriculumIsaacSalazar.pdf"
        download
        class="w-full md:w-auto contacto-item opacity-0 transform translate-y-6"
      >
        <button
          class="w-full md:w-auto flex items-center justify-center gap-2 px-6 py-3 bg-transparent border border-gray-700 rounded-lg text-white hover:bg-red-600/10 hover:border-red-600/50 transition-all duration-300 group"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            viewBox="0 0 24 24"
            class="w-5 h-5 text-white group-hover:text-red-500 transition-colors"
          >
            <path
              fill="currentColor"
              d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8l-6-6m-2 17h-2v-5h2v5m0-7h-2v-2h2v2m1-3h-.5V3.5L17 8h-4z"
            ></path>
          </svg>
          <span
            class="text-sm md:text-base font-light group-hover:text-red-500 transition-colors"
            >Curriculum</span
          >
        </button>
      </a>
    </div>
  </div>
</section>

<script>
  import gsap from "gsap";

  document.addEventListener("DOMContentLoaded", () => {
    const contactoSection = document.getElementById("contact");
    const contactoItems = document.querySelectorAll(".contacto-item");
    const contactoTitle = document.querySelector(".contacto-title");
    const contactoSubtitle = document.querySelector(".contacto-subtitle");
    const prefersReducedMotion = window.matchMedia(
      "(prefers-reduced-motion: reduce)"
    ).matches;

    if (!prefersReducedMotion) {
      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              // Animación del título
              gsap.to(contactoTitle, {
                opacity: 1,
                duration: 0.8,
                ease: "power2.out",
              });

              // Animación del subtítulo
              gsap.to(contactoSubtitle, {
                opacity: 1,
                duration: 0.8,
                delay: 0.2,
                ease: "power2.out",
              });

              // Animación escalonada de los botones
              gsap.to(contactoItems, {
                opacity: 1,
                y: 0,
                duration: 0.6,
                stagger: 0.15,
                ease: "back.out(1.7)",
                delay: 0.4,
              });

              // Reiniciar animaciones cuando el elemento deja de ser visible
            } else {
              gsap.set([contactoTitle, contactoSubtitle, ...contactoItems], {
                opacity: 0,
                y: 20,
              });
            }
          });
        },
        { threshold: 0.1 }
      );

      observer.observe(contactoSection);
    } else {
      gsap.set([contactoTitle, contactoSubtitle, ...contactoItems], {
        opacity: 1,
        y: 0,
      });
    }
  });
</script>

<style>
  .contacto-item {
    transition:
      opacity 0.4s ease,
      transform 0.4s ease;
  }

  /* Efectos hover mejorados */
  button:hover svg {
    filter: drop-shadow(0 0 8px currentColor);
    transform: scale(1.1);
    transition: all 0.3s ease;
  }

  /* Ajustes para móvil mejorados */
  @media (max-width: 768px) {
    #contacto {
      padding-top: 3rem;
      padding-bottom: 3rem;
    }

    .contacto-title {
      font-size: 1.75rem;
      margin-bottom: 1rem;
    }

    .contacto-subtitle {
      margin-bottom: 2rem;
      padding: 0 1rem;
    }

    /* Botones en columna con mejor espaciado */
    .flex-col {
      gap: 1rem !important;
    }

    button {
      width: 100%;
      max-width: 280px;
      padding: 0.85rem 1.5rem;
      margin: 0 auto;
    }
  }
</style>
